# 🏗️ WIKISOFT3 아키텍처

> AI Agent 기반 HR 데이터 자동 검증 시스템의 기술 아키텍처

---

## 📐 전체 시스템 구조

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              사용자 레이어                                    │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  웹 브라우저 (http://localhost:3003)                                  │   │
│  │  • 파일 업로드 (드래그 앤 드롭)                                        │   │
│  │  • 검증 결과 조회                                                     │   │
│  │  • 수동 매핑 수정                                                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└───────────────────────────────────┬─────────────────────────────────────────┘
                                    │ HTTP
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           프레젠테이션 레이어                                  │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  Frontend (React + TypeScript + Vite)                                │   │
│  │  ├─ App.tsx              # 메인 앱, 라우팅                            │   │
│  │  ├─ ValidationResults.tsx # 검증 결과 시각화                          │   │
│  │  ├─ ManualMapping.tsx    # 수동 매핑 UI                               │   │
│  │  └─ ChatBot.tsx          # AI 챗봇 (선택적)                           │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└───────────────────────────────────┬─────────────────────────────────────────┘
                                    │ REST API
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                              API 레이어                                      │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  FastAPI (http://localhost:8003)                                     │   │
│  │                                                                       │   │
│  │  미들웨어:                                                            │   │
│  │  ├─ CORS (localhost만 허용)                                          │   │
│  │  ├─ Rate Limiting (분당 20회)                                        │   │
│  │  └─ 파일 검증 (10MB, xls/xlsx)                                       │   │
│  │                                                                       │   │
│  │  엔드포인트:                                                          │   │
│  │  ├─ POST /api/auto-validate     # 자동 검증                          │   │
│  │  ├─ POST /api/react-agent/validate  # Agent 검증                     │   │
│  │  ├─ GET  /api/diagnostic-questions  # 진단 질문                       │   │
│  │  └─ GET  /api/health            # 상태 확인                           │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└───────────────────────────────────┬─────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                             Agent 레이어                                     │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  ReACT Agent (Think → Act → Observe)                                 │   │
│  │                                                                       │   │
│  │  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐              │   │
│  │  │   Think     │ → │    Act      │ → │  Observe    │ → (반복)      │   │
│  │  │  상황 분석   │    │  도구 실행   │    │  결과 평가   │              │   │
│  │  └─────────────┘    └─────────────┘    └─────────────┘              │   │
│  │                                                                       │   │
│  │  구성요소:                                                            │   │
│  │  ├─ Tool Registry    # 도구 등록/호출 관리                            │   │
│  │  ├─ Confidence Scorer # 신뢰도 계산                                   │   │
│  │  ├─ Retry Strategies # 재시도 전략                                    │   │
│  │  └─ Decision Engine  # 다음 액션 결정                                 │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└───────────────────────────────────┬─────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                              도구 레이어                                     │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  Core Tools (Tool Registry에 등록)                                    │   │
│  │                                                                       │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │   │
│  │  │   Parser    │  │   Matcher   │  │  Validator  │  │  Duplicate  │ │   │
│  │  │             │  │             │  │             │  │  Detector   │ │   │
│  │  │ • XLS/XLSX  │  │ • Few-shot  │  │ • L1 형식   │  │ • 완전 중복  │ │   │
│  │  │ • 빈행 제거  │  │ • AI 매칭   │  │ • L2 교차   │  │ • 유사 중복  │ │   │
│  │  │ • 날짜 변환  │  │ • 규칙 폴백  │  │ • 범위 검사  │  │ • 의심 중복  │ │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘ │   │
│  │                                                                       │   │
│  │  ┌─────────────┐  ┌─────────────┐                                    │   │
│  │  │   Report    │  │    LLM      │                                    │   │
│  │  │  Generator  │  │   Client    │                                    │   │
│  │  │ • JSON 출력  │  │ • GPT-4o   │                                    │   │
│  │  │ • Excel(예정)│  │ • 폴백 처리  │                                    │   │
│  │  └─────────────┘  └─────────────┘                                    │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└───────────────────────────────────┬─────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                             메모리 레이어                                    │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  Case Store (JSON 기반)                                              │   │
│  │                                                                       │   │
│  │  training_data/cases/                                                 │   │
│  │  ├─ index.json           # 182개 헤더 패턴                            │   │
│  │  ├─ {case_id}.json       # 개별 케이스 (42개)                         │   │
│  │  └─ ...                                                               │   │
│  │                                                                       │   │
│  │  기능:                                                                │   │
│  │  ├─ 성공 케이스 자동 저장                                              │   │
│  │  ├─ Few-shot 예제 조회                                                │   │
│  │  ├─ 헤더 패턴 학습                                                    │   │
│  │  └─ 유사도 기반 검색                                                  │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 데이터 흐름

### 1. 파일 검증 요청 흐름

```
사용자: 파일 업로드
         │
         ▼
┌─────────────────┐
│  /api/auto-validate  │
│  (FastAPI)       │
└────────┬────────┘
         │ 1. 파일 검증 (크기, 확장자)
         │ 2. 바이트 읽기
         ▼
┌─────────────────┐
│     Parser      │
│  (parser.py)    │
└────────┬────────┘
         │ 3. Excel → Dict 변환
         │ 4. 빈 행 필터링
         │ 5. 날짜 변환
         ▼
┌─────────────────┐
│     Matcher     │
│  (matcher.py)   │
└────────┬────────┘
         │ 6. Few-shot 매핑 시도
         │ 7. AI 매핑 (필요시)
         │ 8. 규칙 폴백
         ▼
┌─────────────────┐
│    Validator    │
│  (validation)   │
└────────┬────────┘
         │ 9. L1 형식 검증
         │ 10. L2 교차 검증
         │ 11. 중복 탐지
         ▼
┌─────────────────┐
│   Confidence    │
│  (confidence.py)│
└────────┬────────┘
         │ 12. 신뢰도 계산
         │ 13. 이상치 탐지
         ▼
┌─────────────────┐
│     Report      │
│  (report.py)    │
└────────┬────────┘
         │ 14. JSON 리포트 생성
         ▼
    결과 반환
```

### 2. AI 매칭 우선순위

```
입력: 헤더 목록 ["사번", "성명", "생년월일", ...]
         │
         ▼
┌─────────────────┐
│  1. 무시 컬럼?   │ → 참고사항, 비고, Unnamed → 무시
└────────┬────────┘
         │ NO
         ▼
┌─────────────────┐
│  2. Few-shot?   │ → 학습된 패턴에 있으면 즉시 매핑
└────────┬────────┘
         │ NO
         ▼
┌─────────────────┐
│  3. AI 매칭?    │ → GPT-4o 호출 (API 키 있을 때)
└────────┬────────┘
         │ NO (API 키 없음/실패)
         ▼
┌─────────────────┐
│  4. 규칙 폴백   │ → 문자열 유사도 매칭
└────────┬────────┘
         ▼
    매칭 결과 반환
```

---

## 📦 모듈 책임

### external/ (외부 인터페이스)
| 모듈 | 책임 |
|------|------|
| `api/main.py` | FastAPI 앱 생성, 미들웨어, 라우터 등록 |
| `api/routes/validate.py` | 검증 API 엔드포인트 |
| `api/routes/react_agent.py` | Agent API 엔드포인트 |

### internal/ (내부 로직)
| 모듈 | 책임 |
|------|------|
| `agent/react_agent.py` | ReACT 루프 실행, 도구 조율 |
| `agent/tool_registry.py` | 도구 등록/호출 관리 |
| `agent/confidence.py` | 신뢰도 계산, 이상치 탐지 |
| `agent/retry_strategies.py` | 재시도 전략 (지수 백오프, 대안 체인) |
| `ai/matcher.py` | **핵심!** 헤더 매칭 로직 |
| `ai/llm_client.py` | OpenAI API 클라이언트 |
| `ai/knowledge_base.py` | 도메인 지식 (검증 규칙 등) |
| `parsers/parser.py` | Excel/CSV 파싱, 전처리 |
| `parsers/standard_schema.py` | 표준 스키마 정의 (필드, 별칭) |
| `validators/validation_layer1.py` | L1 형식 검증 |
| `validators/validation_layer2.py` | L2 교차 검증 |
| `memory/case_store.py` | 케이스 저장/조회 |
| `generators/report.py` | 리포트 생성 |

---

## 🧠 AI Agent 설계

### ReACT 패턴
```
Think (생각)
  "이 파일은 14개 컬럼을 가지고 있다. 
   먼저 헤더를 파싱하고 표준 스키마에 매핑해야 한다."
    │
    ▼
Act (행동)
  Tool: match_headers
  Input: {headers: [...], sheet_type: "재직자"}
    │
    ▼
Observe (관찰)
  "12개 매핑 성공, 2개 무시됨. 
   신뢰도 85%. 
   다음으로 중복 검사를 진행해야 한다."
    │
    ▼
(반복 또는 완료)
```

### 도구 목록 (Tool Registry)
| 도구 | 입력 | 출력 | 용도 |
|------|------|------|------|
| `parse_roster` | file_bytes | parsed_dict | Excel 파싱 |
| `match_headers` | parsed, sheet_type | matches | 헤더 매칭 |
| `validate_layer1` | df, matches | validation | 형식 검증 |
| `detect_duplicates` | parsed | duplicates | 중복 탐지 |
| `estimate_confidence` | matches, validation | confidence | 신뢰도 |
| `generate_report` | validation | report | 리포트 생성 |

### 신뢰도 계산
```python
confidence = (
    match_confidence * 0.4 +     # 매칭 신뢰도 (40%)
    error_penalty * 0.3 +        # 오류 패널티 (30%)
    completeness * 0.2 +         # 완성도 (20%)
    consistency * 0.1            # 일관성 (10%)
)
```

---

## 🔒 보안 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                    보안 레이어                               │
│                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │    CORS     │  │ Rate Limit  │  │ File Valid  │         │
│  │             │  │             │  │             │         │
│  │ localhost   │  │  20/분      │  │  10MB       │         │
│  │ 127.0.0.1   │  │  429 응답   │  │  xls/xlsx   │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              PII 마스킹                               │   │
│  │  오류 메시지에 실제 데이터 값 노출 방지                   │   │
│  │  예: "홍길동" → "데이터"                                │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 📈 확장 포인트

### 현재 → 다음 단계

| 영역 | 현재 | 다음 |
|------|------|------|
| 저장소 | JSON 파일 | SQLite/PostgreSQL |
| 캐싱 | 없음 | Redis |
| 큐 | In-memory | Redis Queue |
| 배포 | 로컬 | Docker |
| 모니터링 | 로그만 | Prometheus/Grafana |

### n8n 통합 시점 (나중에)
```
현재 아키텍처:
  사용자 → Frontend → FastAPI → Agent → 결과

n8n 통합 후:
  사용자 → Frontend → FastAPI → Agent → 결과
                                   ↓
                              n8n Workflow
                                   ↓
                    ┌──────────────┼──────────────┐
                    ▼              ▼              ▼
                 Slack          Email          DB 저장
                 알림            알림           
```

---

## 🧪 테스트 전략

| 레벨 | 대상 | 도구 |
|------|------|------|
| 단위 | 개별 함수 | pytest |
| 통합 | API 엔드포인트 | pytest + httpx |
| E2E | 전체 흐름 | 수동 (향후 Playwright) |

```bash
# 테스트 실행
pytest tests/ -v

# 특정 모듈
pytest tests/test_parser.py -v
pytest tests/test_matcher.py -v
```

---

## 📊 성능 특성

| 지표 | 현재 값 | 목표 |
|------|---------|------|
| 파일 파싱 | ~500ms | <1s |
| 헤더 매칭 (Few-shot) | ~50ms | <100ms |
| 헤더 매칭 (AI) | ~2s | <3s |
| 전체 검증 | ~2s | <5s |
| 최대 파일 크기 | 10MB | 50MB |
| 최대 행 수 | 5,000 | 10,000 |

---

## 🔧 설정

### 환경 변수
```bash
# .env
OPENAI_API_KEY=sk-...        # AI 매칭용 (없으면 규칙 폴백)
PYTHONPATH=/path/to/WIKISOFT3
```

### 포트 설정
| 서비스 | 포트 | 용도 |
|--------|------|------|
| Backend | 8003 | FastAPI |
| Frontend | 3003 | Vite Dev Server |
