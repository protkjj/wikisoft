# 📁 WIKISOFT3 프로젝트 구조

> 각 디렉토리와 파일의 역할을 상세히 설명합니다.

---

## 🗂️ 전체 구조

```
WIKISOFT3/
├── 📄 README.md              # 프로젝트 개요, 빠른 시작
├── 📄 ARCHITECTURE.md        # 시스템 아키텍처
├── 📄 PROJECT_SPEC.md        # 기능 명세서
├── 📄 PROJECT_STRUCTURE.md   # 이 파일
├── 📄 requirements.txt       # Python 의존성
├── 📄 test_agent.sh          # Agent 테스트 스크립트
│
├── 📂 external/              # 🌐 외부 인터페이스 (API)
├── 📂 internal/              # 🧠 핵심 비즈니스 로직
├── 📂 frontend/              # 🎨 웹 UI (React)
├── 📂 scripts/               # 🔧 유틸리티 스크립트
├── 📂 tests/                 # 🧪 테스트 코드
├── 📂 training_data/         # 📚 학습 데이터 저장소
└── 📂 test_files/            # 📎 테스트용 샘플 파일
```

---

## 🌐 external/ - 외부 인터페이스

> API 엔드포인트와 외부 시스템과의 통신을 담당

```
external/
├── __init__.py
└── api/
    ├── __init__.py
    ├── main.py                 # FastAPI 앱 생성, 미들웨어 설정
    └── routes/
        ├── __init__.py
        ├── validate.py         # POST /api/auto-validate
        ├── react_agent.py      # POST /api/react-agent/validate
        ├── agent.py            # Agent 관련 추가 API
        ├── batch.py            # 일괄 처리 API
        ├── diagnostic_questions.py  # 진단 질문 API
        └── health.py           # GET /api/health
```

### 📄 main.py
```python
# FastAPI 앱 생성
app = FastAPI(title="WIKISOFT3")

# 미들웨어
- CORS: localhost만 허용
- Rate Limiting: 분당 20회
- 파일 검증: 10MB, xls/xlsx만
```

### 📄 routes/validate.py
- **역할**: 파일 업로드 및 자동 검증 API
- **엔드포인트**: `POST /api/auto-validate`
- **처리 흐름**: 파일 수신 → Parser → Matcher → Validator → 결과 반환

### 📄 routes/react_agent.py
- **역할**: ReACT Agent 기반 검증
- **엔드포인트**: `POST /api/react-agent/validate`
- **특징**: Think-Act-Observe 루프로 단계별 처리

---

## 🧠 internal/ - 핵심 비즈니스 로직

> 실제 검증 로직이 구현된 핵심 모듈

```
internal/
├── __init__.py
├── agent/         # 🤖 AI Agent 시스템
├── ai/            # 🧠 AI/ML 로직 (매칭, LLM)
├── parsers/       # 📊 파일 파싱
├── validators/    # ✅ 데이터 검증
├── memory/        # 💾 학습 데이터 저장
├── generators/    # 📝 리포트 생성
├── queue/         # 📬 작업 큐 (향후 확장용)
└── utils/         # 🔧 유틸리티
```

---

### 🤖 internal/agent/ - AI Agent 시스템

```
agent/
├── __init__.py
├── react_agent.py      # ReACT 패턴 구현 (Think-Act-Observe)
├── tool_registry.py    # 도구 등록 및 호출 관리
├── confidence.py       # 신뢰도 점수 계산
└── retry_strategies.py # 재시도 전략 (지수 백오프 등)
```

#### 📄 react_agent.py
- **역할**: ReACT(Reasoning + Acting) 패턴의 AI Agent
- **핵심 클래스**: `ReactAgent`
- **동작 방식**:
  ```
  1. Think: 현재 상황 분석, 다음 액션 결정
  2. Act: Tool Registry에서 도구 선택 및 실행
  3. Observe: 결과 평가, 다음 단계 결정
  4. 반복 또는 완료
  ```

#### 📄 tool_registry.py
- **역할**: Agent가 사용할 도구들을 등록하고 호출
- **등록된 도구**:
  | 도구명 | 기능 |
  |--------|------|
  | `parse_roster` | Excel 파싱 |
  | `match_headers` | 헤더 매핑 |
  | `validate_layer1` | 형식 검증 |
  | `detect_duplicates` | 중복 탐지 |
  | `generate_report` | 리포트 생성 |

#### 📄 confidence.py
- **역할**: 검증 결과의 신뢰도 점수 계산
- **계산 공식**:
  ```python
  score = (
      match_confidence * 0.4 +   # 매핑 신뢰도
      error_penalty * 0.3 +      # 오류 패널티
      completeness * 0.2 +       # 완성도
      consistency * 0.1          # 일관성
  )
  ```

#### 📄 retry_strategies.py
- **역할**: 실패 시 재시도 전략
- **전략 종류**:
  - 지수 백오프 (Exponential Backoff)
  - 대안 체인 (Alternative Chain)
  - 폴백 (Fallback)

---

### 🧠 internal/ai/ - AI/ML 로직

```
ai/
├── __init__.py
├── matcher.py              # ⭐ 핵심! 헤더 매칭 로직
├── llm_client.py           # OpenAI API 클라이언트
├── knowledge_base.py       # 도메인 지식 (검증 규칙)
├── diagnostic_questions.py # 진단 질문 생성
└── dynamic_questions.py    # 동적 질문 생성
```

#### 📄 matcher.py ⭐ (가장 중요!)
- **역할**: 고객사 헤더를 표준 스키마에 매핑
- **핵심 함수**: `match_headers(parsed, sheet_type)`
- **매핑 우선순위**:
  ```
  1. 무시 컬럼 체크 (참고사항, 비고, Unnamed)
  2. Few-shot 매칭 (학습된 패턴)
  3. AI 매칭 (GPT-4o)
  4. 규칙 폴백 (문자열 유사도)
  ```
- **주요 상수**:
  ```python
  IGNORE_HEADERS = [
      "참고", "비고", "구분", "메모",
      "note", "remark", "comment",
      "unnamed", "column", "컬럼"
  ]
  ```

#### 📄 llm_client.py
- **역할**: OpenAI API 호출 래퍼
- **기능**:
  - GPT-4o 모델 호출
  - 에러 처리 및 폴백
  - API 키 없을 때 규칙 기반으로 대체

#### 📄 knowledge_base.py
- **역할**: 도메인 지식 저장
- **내용**:
  - 검증 규칙 정의
  - 필드별 유효성 규칙
  - 에러 메시지 템플릿

---

### 📊 internal/parsers/ - 파일 파싱

```
parsers/
├── __init__.py
├── parser.py           # ⭐ Excel/CSV 파싱
└── standard_schema.py  # 표준 스키마 정의
```

#### 📄 parser.py ⭐
- **역할**: Excel/CSV 파일을 파싱하여 구조화된 데이터로 변환
- **핵심 함수**: `parse_file(file_bytes, filename)`
- **주요 기능**:
  | 함수 | 기능 |
  |------|------|
  | `_find_header_row()` | 헤더 행 자동 탐지 |
  | `_is_empty_row()` | 빈 행 판별 (사원번호 없으면 빈 행) |
  | `_convert_excel_date()` | Excel 직렬번호 → ISO 날짜 |
  | `_is_description_row()` | 설명 행 필터링 |
  | `_select_sheet()` | 적절한 시트 선택 |

- **시트 선택 우선순위**:
  ```
  1. "(2-2) 재직자 명부"
  2. "재직자명부_시스템input"
  3. "재직자" 포함 시트
  4. 첫 번째 시트
  ```

#### 📄 standard_schema.py
- **역할**: 표준 필드 정의
- **내용**:
  ```python
  STANDARD_FIELDS = {
      "employee_id": {
          "aliases": ["사원번호", "사번", "직원번호", "EmpNo"],
          "required": True,
          "type": "string"
      },
      "name": {
          "aliases": ["성명", "이름", "직원명", "사원명"],
          "required": True,
          "type": "string"
      },
      ...
  }
  ```

---

### ✅ internal/validators/ - 데이터 검증

```
validators/
├── __init__.py
├── validator.py            # 통합 검증 인터페이스
├── validation_layer1.py    # L1: 형식 검증
├── validation_layer2.py    # L2: 교차 검증
└── duplicate_detector.py   # 중복 탐지
```

#### 📄 validation_layer1.py
- **역할**: 개별 필드 형식 검증
- **검증 규칙**:
  | 필드 | 규칙 |
  |------|------|
  | `birth_date` | YYYY-MM-DD 형식 |
  | `phone` | 010-XXXX-XXXX 형식 |
  | `email` | RFC 5322 이메일 형식 |
  | `id_number` | 주민등록번호 형식 |

#### 📄 validation_layer2.py
- **역할**: 필드 간 교차 검증
- **검증 규칙**:
  | 규칙 | 설명 |
  |------|------|
  | `hire_date < today` | 입사일은 미래일 수 없음 |
  | `birth_date < hire_date` | 생일이 입사일보다 이전 |
  | `resignation_date > hire_date` | 퇴사일이 입사일 이후 |

#### 📄 duplicate_detector.py
- **역할**: 중복 데이터 탐지
- **중복 유형**:
  | 유형 | 조건 | 레벨 |
  |------|------|------|
  | 완전 중복 | 모든 필드 동일 | Error |
  | 유사 중복 | 사번+이름 동일 | Warning |
  | 의심 중복 | 이름+부서 동일 | Info |

---

### 💾 internal/memory/ - 학습 데이터 저장

```
memory/
├── __init__.py
├── case_store.py     # 케이스 저장/조회
└── persistence.py    # 영속성 관리
```

#### 📄 case_store.py
- **역할**: 성공한 매핑 케이스 저장 및 Few-shot 예제 조회
- **핵심 기능**:
  | 함수 | 기능 |
  |------|------|
  | `save_case()` | 새 케이스 저장 |
  | `get_examples()` | Few-shot 예제 조회 |
  | `find_similar()` | 유사 패턴 검색 |
- **저장 위치**: `training_data/cases/`

---

### 📝 internal/generators/ - 리포트 생성

```
generators/
├── __init__.py
└── report.py         # 리포트 생성
```

#### 📄 report.py
- **역할**: 검증 결과를 리포트로 변환
- **출력 형식**:
  - JSON (현재 지원)
  - Excel with colors (예정 🔶)

---

## 🎨 frontend/ - 웹 UI

> React + TypeScript + Vite 기반 웹 인터페이스

```
frontend/
├── package.json          # npm 의존성
├── tsconfig.json         # TypeScript 설정
├── vite.config.ts        # Vite 설정
├── index.html            # HTML 진입점
└── src/
    ├── main.tsx          # React 진입점
    ├── App.tsx           # 메인 앱 컴포넌트
    ├── App.css           # 앱 스타일
    ├── api.ts            # API 호출 함수
    ├── types.ts          # TypeScript 타입 정의
    ├── theme.css         # 테마 변수
    ├── index.css         # 전역 스타일
    │
    ├── ValidationResults.tsx   # 검증 결과 표시
    ├── ValidationResults.css
    ├── ManualMapping.tsx       # 수동 매핑 UI
    ├── ManualMapping.css
    ├── ChatBot.tsx             # AI 챗봇 (선택적)
    ├── ChatBot.css
    │
    └── components/       # 재사용 컴포넌트
```

### 📄 App.tsx
- **역할**: 메인 앱, 파일 업로드 UI
- **기능**:
  - 드래그 앤 드롭 파일 업로드
  - 검증 결과 표시
  - 탭 네비게이션

### 📄 ValidationResults.tsx
- **역할**: 검증 결과 시각화
- **표시 내용**:
  - 매핑된/미매핑/무시된 헤더
  - 검증 오류 및 경고
  - 중복 데이터
  - 신뢰도 점수

### 📄 ManualMapping.tsx
- **역할**: 수동으로 헤더 매핑 수정
- **기능**:
  - 드롭다운으로 표준 필드 선택
  - 저장 시 학습 데이터에 반영

---

## 🔧 scripts/ - 유틸리티 스크립트

```
scripts/
├── generate_samples.py   # 샘플 데이터 생성
└── learn_from_file.py    # 파일에서 학습
```

### 📄 learn_from_file.py
- **역할**: Excel 파일을 분석하여 케이스로 저장
- **사용법**:
  ```bash
  python scripts/learn_from_file.py /path/to/file.xlsx
  ```
- **처리 흐름**:
  1. 파일 파싱
  2. 헤더 매핑
  3. 케이스 저장 (training_data/cases/)

### 📄 generate_samples.py
- **역할**: 합성 샘플 데이터 생성
- **용도**: 테스트 및 학습 데이터 증강

---

## 🧪 tests/ - 테스트 코드

```
tests/
├── __init__.py
├── test_api.py               # API 엔드포인트 테스트
├── test_parser.py            # 파서 테스트
├── test_matcher.py           # 매칭 테스트
├── test_validator.py         # 검증 테스트
├── test_duplicate_detector.py # 중복 탐지 테스트
└── test_react_agent.py       # Agent 테스트
```

### 실행 방법
```bash
# 전체 테스트
pytest tests/ -v

# 특정 모듈
pytest tests/test_parser.py -v
pytest tests/test_matcher.py -v
```

---

## 📚 training_data/ - 학습 데이터

```
training_data/
├── README.md
└── cases/
    ├── index.json        # 헤더 패턴 인덱스 (182개)
    ├── {case_id}.json    # 개별 케이스 (42개)
    └── ...
```

### 📄 index.json 구조
```json
{
  "사원번호": ["employee_id", 0.95],
  "사번": ["employee_id", 0.98],
  "성명": ["name", 0.99],
  "이름": ["name", 0.97],
  ...
}
```

### 📄 케이스 파일 구조
```json
{
  "id": "case_abc123",
  "company": "회사명",
  "created_at": "2026-01-01T00:00:00",
  "headers": ["사번", "성명", "입사일", ...],
  "mappings": {
    "사번": "employee_id",
    "성명": "name",
    ...
  },
  "confidence": 0.92
}
```

---

## 📎 test_files/ - 테스트용 샘플

```
test_files/
├── sample_roster.xlsx    # 기본 테스트 파일
├── complex_headers.xlsx  # 복잡한 헤더 테스트
└── ...
```

---

## 🔗 파일 간 의존성

```
                    ┌─────────────────────┐
                    │   external/api/     │
                    │     main.py         │
                    └──────────┬──────────┘
                               │
                    ┌──────────▼──────────┐
                    │   routes/           │
                    │   validate.py       │
                    └──────────┬──────────┘
                               │
          ┌────────────────────┼────────────────────┐
          │                    │                    │
          ▼                    ▼                    ▼
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│  parsers/       │ │     ai/         │ │  validators/    │
│  parser.py      │ │  matcher.py     │ │  validator.py   │
└────────┬────────┘ └────────┬────────┘ └────────┬────────┘
         │                   │                   │
         │          ┌────────▼────────┐          │
         │          │    memory/      │          │
         └─────────►│  case_store.py  │◄─────────┘
                    └─────────────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │  training_data/ │
                    │    cases/       │
                    └─────────────────┘
```

---

## 📌 핵심 파일 요약

| 중요도 | 파일 | 역할 |
|--------|------|------|
| ⭐⭐⭐ | `internal/ai/matcher.py` | 헤더 매핑 (핵심 로직) |
| ⭐⭐⭐ | `internal/parsers/parser.py` | Excel 파싱 |
| ⭐⭐ | `internal/agent/react_agent.py` | AI Agent |
| ⭐⭐ | `internal/memory/case_store.py` | 학습 데이터 관리 |
| ⭐⭐ | `external/api/routes/validate.py` | API 엔드포인트 |
| ⭐ | `internal/validators/validator.py` | 데이터 검증 |
| ⭐ | `frontend/src/App.tsx` | 웹 UI |
