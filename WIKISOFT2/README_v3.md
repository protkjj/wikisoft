# 🚀 WIKISOFT2 - AI Agent 기반 HR 명부 자동 검증 시스템

**현재 버전**: 2.1 (검증 기능 완성)  
**목표 버전**: 3.0 (완전자율 AI Agent, 2025년 Q2)  
**최종 비전**: 4.0 (멀티 도메인 자동화, 2025년 Q3)

AI 기반 Excel 헤더 매칭 + 자동 검증 + 신뢰도 기반 의사결정 + 완전자율 Agent

---

## 📋 목차

- [현재 상태](#현재-상태-v21)
- [미래 로드맵](#미래-로드맵-v30--40)
- [빠른 시작](#빠른-시작)
- [주요 기능](#주요-기능)
- [아키텍처](#아키텍처)
- [API](#api)
- [개발 가이드](#개발-가이드)

---

## 현재 상태 (v2.1)

### ✅ 완료된 기능
- ✅ AI 헤더 매칭 (GPT-4o + 폴백)
- ✅ Layer 2 교차 검증 (챗봇 vs 명부)
- ✅ Excel 경고 시스템 (색상 + 코멘트)
- ✅ API 토큰 검증 (Bearer token)
- ✅ 파일 타입 검증 (5단계)
- ✅ 구조화된 로깅 (JSON)
- ✅ Health Check 엔드포인트

### ⚠️ 한계
- ❌ 사용자가 모든 질문(28개)에 직접 답변 필요 (자동화 불가)
- ❌ 신뢰도 모델 부재 (AI가 의사결정 못함)
- ❌ Tool Registry 없음 (모놀리식 구조)
- ❌ Memory 시스템 없음 (학습 불가)
- ❌ 의사결정 자동화 불가

### 📊 성능 지표
```
처리 시간: 30-60분/파일
사람 개입: ~100% (모든 질문에 답변)
신뢰도: ~70% (폴백 모드 포함)
확장성: 선형 (경험 증가 안 함)
```

---

## 미래 로드맵 (v3.0 + 4.0)

### Phase 1: Agent-Ready Foundation (v2.2, 2주)
```
🎯 목표: Tool Registry + ReACT 루프 기초
- Tool Registry 구축 (도구 중앙 관리)
- ReACT Loop 함수 기반 구현
- Confidence Scorer 모델 (신뢰도 계산)
- Decision Engine (의사결정 자동화)
- Agent API 인터페이스 추가

결과: 반자율 Agent (20-30% 수동 개입)
```

### Phase 2: Intelligence (v3.0, 2개월)
```
🎯 목표: 완전자율 Agent + Learning
- LangChain/LlamaIndex 통합
- Few-shot learning (상황별 프롬프트)
- Memory 시스템 (Redis + Vector DB)
- Human-in-the-loop UI
- Batch processing

결과: 준자율 Agent (10-15% 수동 개입)
시간: 5-10분/파일 (기존 대비 5배 빠름)
```

### Phase 3: Autonomy (v4.0, 3개월)
```
🎯 목표: 완전자동화 + Cross-domain
- 자동 리플래닝
- Cross-file 학습 (여러 파일 경험)
- 자체 검증 메커니즘
- Batch 자동 처리 (1000+ 파일/일)
- 새로운 도메인 자동 확장

결과: 완전자율 Agent (5% 이하 수동 개입)
시간: 1분/파일 (기존 대비 60배 빠름)
신뢰도: 95%+ (전문가 수준)
```

---

## 빠른 시작

### 1. 설치

```bash
# 저장소 클론
cd /Users/kj/Desktop/wiki/WIKISOFT2

# 가상환경
python3 -m venv .venv
source .venv/bin/activate

# 의존성 설치
pip install -r requirements.txt
```

### 2. 환경 설정

```bash
# .env 파일 생성
cat > .env << EOF
# API 보안
API_TOKEN=wikisoft2-secret-2025

# OpenAI (선택, AI 헤더 매칭용)
OPENAI_API_KEY=sk-proj-...

# 세션 관리
MAX_SESSIONS=50
SESSION_TTL_MINUTES=120
EOF
```

### 3. 서버 실행

```bash
# 개발 모드
python -m uvicorn external.api.main:app --reload --port 8000

# 프로덕션 모드
export API_TOKEN=wikisoft2-secret-2025
python -m uvicorn external.api.main:app --host 0.0.0.0 --port 8000
```

### 4. Health Check

```bash
# 헬스 체크
curl http://localhost:8000/health | jq .

# 진단 질문 조회 (토큰 필수)
curl -H "Authorization: Bearer wikisoft2-secret-2025" \
     http://localhost:8000/diagnostic-questions | jq . | head -30
```

---

## 주요 기능

### 1️⃣ AI 헤더 매칭 (자동 표준화)

**문제 해결**: 고객사별 다른 Excel 헤더 형식
```
입력:        처리:          결과:
"사번"  →   AI 매칭   →  "사원번호" ✅
"이름"  →   (95%+)    →  "이름" ✅
"생일"  →              "생년월일" ✅
```

**정확도**:
- AI (GPT-4o): 95-100%
- 폴백 (문자열): 70-90%

### 2️⃣ 자동 검증 & 교차 검증

**현재**: 챗봇 질문 필수 → 사용자 개입 필수  
**미래**: AI Agent가 자동 판단 → 필요시만 사람에게 문의

```
현재 (v2.1):
파일 → 파싱 → [챗봇 28개 질문] ← 사람이 답변 필수
        ↓
      검증 → Excel 생성

미래 (v3.0+):
파일 → 파싱 → [AI Agent 자동 판단]
        ↓         (신뢰도 기반)
      검증 ──→  사람 확인 필요?
        ↓         NO: 자동 완료
      Excel     YES: 질문만 제시
      생성
```

### 3️⃣ 신뢰도 기반 의사결정

```
신뢰도 95%+ → 자동 완료
신뢰도 80-95% → 자동 수정 (리뷰 권장)
신뢰도 70-80% → 사람에게 확인 후 수정
신뢰도 <70% → 수동 검토 필수
```

### 4️⃣ 시각적 경고 시스템

```
🔴 빨간색: 신뢰도 낮음 (5% 초과 오차)
🟡 노란색: 신뢰도 중간 (5% 이내 오차)
💬 코멘트: 마우스 오버 시 상세 정보
```

---

## 아키텍처

### 현재 구조 (v2.1)

```
┌─────────────────────────────────────────┐
│          FastAPI (main.py)              │
│     ❌ 모놀리식 (단일 파일)            │
└─────────────────────────────────────────┘
         ↓                ↓
┌──────────────┐  ┌──────────────┐
│  파일 검증   │  │  데이터 처리 │
│  (Token)     │  │  (파싱, 매칭)│
└──────────────┘  └──────────────┘
         ↓                ↓
┌────────────────────────────────┐
│    검증 (Layer 1, 2)           │
└────────────────────────────────┘
         ↓
┌────────────────────────────────┐
│    Excel 생성 + 경고 시스템    │
└────────────────────────────────┘

문제점:
- Agent 로직 추가 어려움
- 도구 재사용 불가능
- 메모리 시스템 없음
- 학습 불가능
```

### 미래 구조 (v3.0+)

```
┌──────────────────────────────────────────────┐
│      AI Agent Orchestration Layer            │
│   (ReACT Loop + Decision Engine)            │
│   🟢 자동 의사결정                          │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│      Tool Registry (중앙 도구 관리)          │
├──────────────────────────────────────────────┤
│  • parse_excel                               │
│  • validate_data                             │
│  • detect_anomalies                          │
│  • auto_correct                              │
│  • estimate_confidence                       │
│  • generate_report                           │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│   Core Processing Layer (현재 모듈들)        │
│  • Excel Parser                              │
│  • Data Validator                            │
│  • Anomaly Detector                          │
│  • Report Generator                          │
└──────────────────────────────────────────────┘
           ↓
┌──────────────────────────────────────────────┐
│  Memory & Persistence Layer                  │
│  🟢 학습 & 경험 축적                         │
├──────────────────────────────────────────────┤
│  • Redis: Session + Decision Log             │
│  • Vector DB (Chroma): Pattern Memory        │
│  • PostgreSQL: Audit Trail                   │
└──────────────────────────────────────────────┘

개선점:
+ Tool 재사용 가능
+ Agent 로직 추가 용이
+ Memory 시스템으로 학습
+ 신뢰도 기반 의사결정
+ 자동 스케일링
```

---

## API

### 현재 엔드포인트 (v2.1)

```bash
# Health Check
GET /health

# 진단 질문 조회
GET /diagnostic-questions
-H "Authorization: Bearer <TOKEN>"

# 검증 실행 (사람의 답변 필수)
POST /validate-with-roster
-F "file=@명부.xlsx"
-F 'chatbot_answers={"q21": 17, ...}'

# Excel 생성
POST /generate-with-validation
-F "session_id=abc123..."
-F "company_name=세라젬"
```

### 미래 엔드포인트 (v3.0+)

```bash
# 완전자율 검증 (파일만)
POST /auto-validate        🆕
-F "file=@명부.xlsx"
-F "confidence_threshold=0.8"  # 신뢰도 임계값
→ {
    "status": "success" | "needs_review" | "failed",
    "confidence": 0.95,
    "decisions": [...],
    "human_inputs": [...],      # 사람이 답변한 것들만
    "report": "<HTML>"
  }

# 배치 처리 (대량 파일)
POST /batch-validate       🆕
-F "files=@[여러 파일]"
-F "mode=async"            # 비동기 처리
→ { "job_id": "job-123", "status": "processing" }

# Agent 상태 조회
GET /agent/{session_id}    🆕
→ {
    "state": "thinking" | "deciding" | "waiting_human" | "complete",
    "progress": 0.75,
    "next_action": "asking_human",
    "question": {...}
  }
```

---

## 개발 가이드

### 현재 개발 환경 설정

```bash
# 1. 저장소 클론
cd /Users/kj/Desktop/wiki/WIKISOFT2

# 2. 가상환경
python3 -m venv .venv
source .venv/bin/activate

# 3. 의존성
pip install -r requirements.txt

# 4. 테스트
python test_layer2_integration.py
```

### Agent-Ready 개발 (v2.2+)

```bash
# 1. Tool Registry 이해하기
# internal/tools/registry.py 참고

# 2. ReACT Loop 이해하기
# internal/agent/react_loop.py 참고

# 3. 새로운 도구 추가
# internal/tools/my_tool.py 작성
# Tool Registry에 등록

# 4. Agent 테스트
python -m pytest tests/agent/
```

### 구조 변경 계획 (v2.2)

```
현재:
external/api/main.py (815줄 모놀리식)

미래:
external/api/
├── main.py (100줄, API 초기화만)
├── routes/
│   ├── validation.py (검증 API)
│   └── agent.py (Agent API)
├── internal/
│   ├── tools/
│   │   ├── registry.py
│   │   ├── parser.py
│   │   ├── validator.py
│   │   └── ...
│   └── agent/
│       ├── react_loop.py
│       ├── decision_engine.py
│       ├── confidence.py
│       └── memory.py
```

---

## 📚 문서

| 문서 | 대상 | 내용 |
|------|------|------|
| **README.md** (현재) | 모두 | 프로젝트 개요, 로드맵 |
| **PROJECT_SPEC.md** | 개발자 | API 명세, 기술 스펙 |
| **PROJECT_OVERVIEW.md** | PM/비기술진 | 비즈니스 가치, 성능 |
| **REBUILD_GUIDE.md** | 신규 개발자 | 설치, 실행, 개발 방법 |
| **AGENT_ROADMAP.md** 🆕 | 아키텍트 | Agent 설계, 마일스톤 |

---

## 🔗 관련 링크

- [Project Spec](PROJECT_SPEC.md) - 기술 명세서
- [Project Overview](PROJECT_OVERVIEW.md) - 프로젝트 개요
- [Rebuild Guide](REBUILD_GUIDE.md) - 개발 가이드
- [Agent Roadmap](AGENT_ROADMAP.md) - Agent 로드맵

---

## 📝 라이선스

MIT License

---

**최종 목표**: HR 명부 검증 자동화 → AI 기반 자동화 플랫폼 확장  
**예상 일정**: 2025년 Q2 (v3.0), Q3 (v4.0)
